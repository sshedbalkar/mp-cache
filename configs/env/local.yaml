# Local environment bootstrap example.
# Materialize or copy this into configs/bootstrap.yaml when you want explicit
# local settings checked against the defaults.

service:
  environment_name: local
server:
  socket_path: .tmp/run/mp-cache.sock
  pid_file_path: .tmp/run/mp-cache.pid
