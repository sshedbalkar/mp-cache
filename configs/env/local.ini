# Local environment bootstrap example.
# Materialize or copy this into configs/bootstrap.ini when you want explicit
# local settings checked against the defaults.

[service]
environment_name = local

[server]
socket_path = /tmp/mp-cache/run/mp-cache.sock
