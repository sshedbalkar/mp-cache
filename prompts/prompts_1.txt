I need to build a cache server using the C programming language. The project folder would be 'multi-player-app/mp_cache'. Follow relevant engineering standards from the 'multi_player_app_ai' project. Specifications:
1. Use Unix socket for local conection, local deployment
2. Expose the socket to network over SSH tunnel through proxy server such as Nginx
3. Data backed by local file storage, main cache remains in memory
4. Data is not manipualted in any way during processing and while in storage
5. Data is encrypted while in storage
6. All data has TTL of 172,800 sec unless a different TTL is set (while storing data)
7. Performance, security, no vulnerability are the key objectives
8. Should be configurable using a bootstrap config file. Default values are hardcoded in the project which will be utilized if a bootstrap config file is not found and the same gets exported as a bootstrap config file for next time use (it has comments to explain what each configuration means, valid values, etc).
9. Should be a long running process.
10. Current data state could be exported into several data files (limit set in config file) and each file could be independently imported into another instance of a cache server. In v2, we could add the ability to prioritize all data from the same project/module into the same data export file.
11. Timezone is UTC.
12. This would be a S2S application.
13. Could be started, stopped, and restarted gracefully using a script.
14. Uses the 'mp_logger' git submodule for logging.
15. Has permission groups: admin, client, operator. 'admin' has access to all of the APIs.
16. A new client can be registered by the 'admin' and an API access token is created for them. This API token could be invalidated and a new one could be generated by the 'admin' (provide APIs for the same).
17. Exposes APIs Get, Set, Delete, Health to 'client' permission group.
18. Exposes other APIs such as 'Memory usage', 'Export data to file', 'Import Data file', 'Purge all data', 'Purge selected keys', 'up time' (health and data management, access management APIs), etc to the 'admin' permission group.
19. Deployment environment would be a Linux system.
20. Environments: Local, Development, QA, Staging, Production.
21. Build promotion strategy to be used.
22. Local environment is Arch-CachyOS.
23. Max memory for cache use can be configured through the config file.
24. Runtime overhead should be minimal.
25. Cache set/fetch functions should be very optimized (for clients such as a realtime game)
26. Application by itself should be stateless. All of the important state should be exportable and importable through files.
27. Keys and Data should be bounded.
28. Create scripts for build, deployment (including local), promotion, testing, validation, etc.
29. Create Architecture plan, once approved create the project.
30. Create QoL scripts, methods wherever needed.
31. Store mp_cache logs in the root/logs of the deployment folder. These logs are accessible through the 'logs' API with relevant configurable params.
32. 'operator' has access to all health, logs, client management APIs.
33. Try to use similar folder structure as 'multi-player-app-ai' project.
Done

Approved. Deafult TTL should be configurable through the config file. Also create context folder similar to 'multi-player-app-ai' project. Create README, AGENTS, files. Create a basic README at the root of the folder 'multi-player-app' folder to route to the child projects. Done

Tell me exact commands to run from a normal local shell, update the commands in a doc for manual testing. In the same doc for manual tetsing give curl commands to test every single verifiable thing including all of the APIs. Take sample data. Mention what set up is needed, how can secretes be created/deployed for the build to run, any other pre-condition for the build to run, etc. Create a comprehensive doc for end to end manual testing. Done

Check if Phase 2, 3, and 4 of the 'mp-cache/docs/architecture/mp-cache-architecture.md' are implemented or not. Make sure they are implemented, tested, verified, context is updated. Done.

For the 'mp-cache' project, use this as a durable rule: "All public data structures, functions, classes, etc need to have proper comments (follow a commenting standard, declare the format in docs for later reuse). All non-trivial non-public functions, data structures, classes shuld also carry appropriate comments in the same format". Scan the whole project to check if the rule is met or not, if it's not then make sure you add comments to all applicable functions, data structures, classes, etc. Verify that this is done. Done

Naming and durable rule. Done

The rule is to be followed by any change in 'mp-cache/internal/' as well. Update the rule set. And check all of the files in the 'internal/' directory and make sure the rule is followed. Update docs/context/tests/etc. Build, deploy, and validate that the cache server is properly working. Done

For 'mp-cache' project, implement: selective key-purge API, explicit token invalidation beyond rotation. Update docs, context, tests, validation etc. Make sure to build, deploy, test, and validate the result. Done

Valgrind check - rule for any c based native development. Done

In 'mp-cache', use the 'mp_logger' submodule for any kind of logging needs. Make sure this is treated as a durable rule and followed in the future as well. Done

Make sure the project 'mp_cache' can be deployed and tested (all tests) rootless, except for the valgrind test. Done

Create a doc on the host setup, what was the issue with Valgrind,what was the root cause, how was it fixed. This doc should be reusable in the future, if needed. Place it in 'docs/'. Done

-Wall, -Wextra, -Wpedantic fix. Done

Trying to build, deploy, run, test, and validate the 'mp-cache' project and this is the final report: "Built and validated 'mp-cache'. Two environment-specific issues remain. First, the default local deploy path still fails if you reuse the repo’s current state.checkpoint with a different MP_SECRET_LOCAL_STORAGE_KEY; to fix that, either reuse the original storage key or clear state.checkpoint and state.journal before rerunning make deploy-local. Second, direct Unix-socket bind/connect is denied inside this Codex sandbox, so I had to validate the live smoke test outside the sandbox; on a normal local shell that restriction should not apply." How can this be fixed? Done

In 'mp-cache' project, make sure the doc 'mp-cache/docs/runbooks/manual-end-to-end-testing.md' is up to date and update it as if it's for the audience of novice developers. It's not clear enough as it is. Handhold every step of the way. Done

Rate limiting system.

Self-written memory manager integration.

In 'mp-cache', just like the doc 'mp-cache/docs/runbooks/manual-end-to-end-testing.md', create another doc where the setu and deployment is done through a script, but only the APIs are tested manually. The 'mp-cache/docs/runbooks/manual-end-to-end-testing.md' and the new doc can refer to each other at relevant places. The goal of the new doc is to rapidly test the APIs manually and take care of the pre-setup, setup, deployment, etc through script. Reuse existing scripts/docs as much as possible. If required, modularize the scripts so that they can be reused. Don't create copies of the same script or even sections of the same script. Update context, docs, tests, etc. Done

When I go through 'mp-cache/docs/runbooks/manual-api-testing-with-scripted-local-deploy.md' and run the script block in section 6 in a normal bash sell, I get the error: "bash: status_str: unbound variable". Please fix it. Done

In 'mp-cache' project, make it a durable rule that anytime an application error is being sent as a response, it should also send an associated error code (defined by the project), error descriptions are for humans only, the clients use the error codes to decide how to deal with the error. Scan the codebase and make sure this rule is being followed totally. Update the docs/context/tests, etc. Validate that this is being done. Done

.tmp folder cleanup?

In 'mp-cache' project, constants, appropriate readonly values, appropriate default values, appropriate config values, etc should be defined in one place for easier discovery and update. Make sure this is followed as a durable rule. Don't scatter them around in multiple source files across the project. Update docs/context/tests wherever required. Perform validation. Done

Make sure all of the API endpoints like '/v1/status', /v1/health', etc are also moved to a central place. We don't want those to be scattered across the project multiple times as well. This also needs to go in the durable rule. Update, check, and validate. Done

---
In the project 'multi-player-app-ai', constants, readonly values, default values, config keys, environment variable names, protocol markers, HTTP/API routes, HTTP error codes, status names, reusable capacities/limits, date formats, and similar durable values must be defined in one central source-of-truth location for easier discovery and update.

Please inspect the project first, identify the existing best central location or create one that fits the project’s architecture. Then refactor code, tests, scripts, and local docs/context so those durable values are consumed from that central place instead of being scattered across source files.

Also make this a durable rule:
- Update the project’s engineering standards/docs/context to state that these values must be centralized.
- Add or update an automated validation/check so future scattered constants, route literals, error-code literals, or default values fail the standards check.
- Keep public API docs/runbooks as contract documentation, but avoid treating duplicated literals in executable code/tests/scripts as acceptable.

Scope carefully:
- Preserve existing behavior.
- Do not centralize local loop counters, one-off test data, obvious arithmetic, or private temporary variables unless they represent a reusable project value.
- Do not overwrite unrelated user changes.
- Follow the project’s existing naming, folder, and validation conventions.

After implementation, double-check with searches for scattered literals, especially:
- `#define`, `const`, `static const`, enums/status-name strings
- default paths, ports, limits, TTLs, capacities
- config section/key names
- env var names and secret reference prefixes
- API endpoints/routes such as `/v1/...`
- API error codes such as `not_found`, `invalid_argument`, `unauthorized`
- diagnostic status strings such as `OK`, `UNKNOWN`, `LIMIT_EXCEEDED`

Update relevant tests so they assert against the central constants rather than copied literals.

Finally, run the project’s required validation suite, including unit tests, standards/context checks, naming checks, and memory/sanitizer/Valgrind checks where the project requires them. Report exactly what changed and the validation results, including any environment blockers.
Done
---

Deploy 'mp-cache' for use in local as a auto-start service that uses the build files from the 'mp-cache/build' directory. Configure it to work behind the Nginx proxy server running on local. Create a local deploy script (with detailed comments) that does all of this (first time deployment and recurring deployment). Create similar scripts for deploying on Dev/QA/Staging/Production environments. Create docs that explain the local and remote deployment process (update if there is one already). Update docs/context/tests and validate. Done

make sure the 'mp-cache/deploy/systemd/mp-cache.service' starts after the SSD volume is mounted. Done

Give various (wherever applicable) usage examples for all of the scripts within 'mp-cache/scripts' within it's top level comments. Make this a durable rule for all future and updated scripts. Done.

Make sure all of the scripts have 'print usage' functionality as well which gets triggered on '--help' switch. Make this also a durable rule. Done

nginx paths should not be hardcoded, use existing appropriate config file (define nginx paths as constants) or create a new config file and use it in all the places where the paths are used. Done

Do the "usage examples in header comments" and implementing the '--help' option in all scripts for 'mp-cache/native/mp_logger' sub-project and 'multi-player-app-ai' project as well. make sure all of their scripts follow it. Add the changes as independent durable rules in their own ruleset and update their docs/context/tests as well. Done

---

In 'mp-cache' project, consider the local deployment script, 'mp-cache/scripts/deploy-local.sh', does it generate a deployment report as to how successful the deployment went, if not create a report and store it in an appropriate place. Also, once the local deployment is done how can the post-deployment tests be done to make sure if everything is working as intended and geberate a report associated with it. Follow the existing standards within the project. Update the top comment, docs/tests/context/validations/etc associated with this. Done

In 'mp-cache', a lot of the configuration parameters in the script files have been either partially or fully hardcoded into the script files, we cannot have that. Move them to a centralized configuration file (use existing ones wherever applicable). This must be a durable rule, update docs/context/tests/validation etc. Done

Create a versioning pattern for the 'mp-cache' project: [MAJOR.MINOR.HOTFIX]. Make the build version configurable using the server config file. By default whenever a new build is created, the build script should automatically update the version number (increements MINOR version) from the config file (unless an option is provided when running the build script which will skip the auto increment and just take the version from the config file as it is - useful in cases the version is updated manually) and the artifact creating script reads the version from config and uses it, appends it to the artifact package. Make this (version pattern to be followed for a build) a durable rule by adding it to the standards. Done

Build version also appears as a part of the metadata of each and every API response.

'mp-cache' should load its nested native/mp_logger/configs/logger.bootstrap.yaml, then apply parent values from configs/bootstrap.yaml. Update docs, context, tests, and verify this.